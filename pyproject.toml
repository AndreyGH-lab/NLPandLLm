[project]
name = "myproject"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "black>=25.9.0",
    "flake8>=7.3.0",
    "pre-commit>=4.3.0",
    "pre-commit-uv>=4.1.5",
    "pytest>=8.4.2",
    "torch>=2.8.0",
]
